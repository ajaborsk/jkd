<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>{{pagetitle}}</title>
{%- for css in css_list %}
  <link rel="stylesheet" href="/static/css/{{css}}"/>
{%- endfor %}
{%- for script in scripts %}
  <script src="/static/scripts/{{script}}"></script>
{%- endfor %}
</head>
<body>
  <h1>{{pagetitle}}</h1>
{{html}}
  <p>{{name}}</p>
  <!-- Le reste du contenu -->
  <p id="test1">Blabla</p>
  <div id="jkd_contents">
  <p>Composition de la page :</p>
  <ol>
  {% for element in parts%}
  <li><b>{{element.jkd_class}}:</b> {{element}}</li>
  {% endfor %}
  </ol>
  </div>
  
<div id="example-table"></div>  
  
  <div>
    <span id="push1">Launch : </span>
    <span id="result1">Result</span>
    <canvas id="chart1" width="800" height="200"></canvas>
  </div>
  <div>
    <span id="push2">Launch : </span>
    <span id="result2">Result</span>
  </div>
  <div id="status_bar"><span id="conn_status">Connecting...</span></div>
<script>

$(document).ready(function()
 {
 
 
 //create Tabulator on DOM element with id "example-table"
$("#example-table").tabulator({
    //height:205, // set height of table (in CSS or here), this enables the Virtual DOM and improves render speed dramatically (can be any valid css height value)
    layout:"fitColumns", //fit columns to width of table (optional)
    columns:[ //Define Table Columns
        {title:"Name", field:"name", width:150},
        {title:"Age", field:"age", align:"left", formatter:"progress"},
        {title:"Favourite Color", field:"col"},
        {title:"Date Of Birth", field:"dob", sorter:"date", align:"center"},
    ],
    rowClick:function(e, row){ //trigger an alert message when the row is clicked
        alert("Row " + row.getData().id + " Clicked!!!!");
    },
});

var tabledata = [
    {id:1, name:"Oli Bob", age:"12", col:"red", dob:""},
    {id:2, name:"Mary May", age:"1", col:"blue", dob:"14/05/1982"},
    {id:3, name:"Christine Lobowski", age:"42", col:"green", dob:"22/05/1982"},
    {id:4, name:"Brendon Philips", age:"125", col:"orange", dob:"01/08/1980"},
    {id:5, name:"Margret Marmajuke", age:"16", col:"yellow", dob:"31/01/1999"},
];

//load sample data into the table
$("#example-table").tabulator("setData", tabledata);

 /*
  send = function(msg)
   {
    websocket.send(JSON.stringify(msg));
   };
*/
 

  var jkd_env = new JkdEnv("chuap", "ws://" + window.location.host + "/ws", "/chuap/homepage");

/*
  $("#push1").click(function(evt)
   {
    env.get('/tabulator_format/output', {},
      function (msg, client)
       {
        $('#result1').text(msg.reply.query);
        var cols = [];
        for (var c=0 ; c < msg.reply.cols.length; c++)
         {
          cols.push({title:msg.reply.cols[c]['title'], field: 'c' + c});
         }
        $("#example-table").tabulator("destroy");
        $("#example-table").tabulator({layout:"fitColumns", columns:cols});
        var tableData = [];
        for (var r= 0; r < msg.reply.data.length; r++)
         {
          var row = msg.reply.data[r];
          var rowData = {id:r};
          for (var c=0 ; c < row.length; c++)
           {
            rowData['c'+c] = row[c];
           }
          tableData.push(rowData);
         }
        $("#example-table").tabulator("setData", tableData);
       },
      null);
   }); */
   
{{js}}

  // Launch jkd environment
  jkd_env.run();

});
</script>
</body>
</html>
